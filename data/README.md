# 历史数据

- [`history_results.json`](history_results.json)：历史提交及测试点结果的快照。
- [`key_kernels.json`](key_kernels.json)：若干候选源码的归档，名称中的 pass 字样只描述该历史候选的记录，不代表当前可提交版本。

当前可靠源码始终以仓库根目录 `kernel.asc` 及根目录 `README.md` 所列提交 ID、SHA-256 为准。这里的 JSON 用于比较与追溯，不应作为自动提交来源。
