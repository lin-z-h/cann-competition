# BatchMatmulMaxSum：20 次架构级优化实验计划

更新时间：2026-09-21

执行状态（2026-09-21）：已在登录浏览器核对线上对数评分公式，当前排行榜显示 `384622` 得分 `20.61`、第 67 名；本地保留的可靠候选 `384543` 按同公式约 `20.46`。尝试 01/02 因目标 case 特征与路由证据不足暂缓；相关的小 M GEMV 扩围受控实验 `394284` 虽 15/15 Pass，但重点 case 5 `98.48→98.85 us`，已回退，不再扫阈值。03/04 因跨 M 超块的活跃核损失与输出 stride 未验证暂缓；05 已由 `384543` 验证并保留，15/15 Pass，case 11（0-based）连续两次约 18% 耗时收益；case 13 单次快 11.4% 但路由未证实，不归因；07 已被现有连续 N-group 架构覆盖，不重复提交，06/08 因无剩余瓶颈证据暂缓；10 的窄路由 C 双缓冲已由 `390882` 实测 15/15 Pass 但 `20.328 < 20.471`，未达到目标、已回退；11 的高阶 AR 归约在 A2/A3 只接受 32 字节对齐内轴，基本 masked 归约需另行解决跨行输出布局与临时空间，暂缓；12 在 `384622` 使明确命中的 case 2 退化 18.3%，已回退，并按依赖取消 13–16；17/18 因缺实际 tiling 与 MTE2/L1 证据暂缓，19 因缺低并行度大 K case 证据暂缓。当前可靠候选 SHA-256 为 `da2f4f2be9c95c06e98c406f19674f0276cf7f4ac69372d1192c8f44a1ed312b`。

## 1. 目标与基本判断

`30` 只是第一阶段参考线，不是最终目标。当前可靠基线是提交 `379068`，15/15 Pass，源码 SHA-256 为：

```text
0d26f7b3e0582888db430d0a2a07b1f691fa0d6b60844932ac8dd250ed6f1922
```

该版本 15 个 case 的耗时为：

```text
11.93, 22.01, 29.39, 16.00, 12.80,
101.08, 17.17, 103.07, 137.61, 147.33,
230.67, 224.88, 32.65, 35.64, 37.27 us
```

按官方题面公式 `100 / (1 + log_1.5(current_time / best_time))` 和提交详情的 15 个 `best_time` 重算，`379068` 平均约 `20.124`，当前可靠候选 `384543` 平均约 `20.471`。此前用 `100 × best_time / current_time` 算出的相近总分纯属巧合，单 case 收益判断并不正确。最值得投入的不是已经接近较优的 case 6/8/9，而是以下两组：

- 第一优先级：case 1、2、5、12，当前单点按官方公式约为 `13.40、13.71、11.09、11.36`；需要改变计算路径或数据复用方式，微调指令很难填平差距。
- 第二优先级：case 0、7、10、11，当前单点约为 `15.33、18.53、19.98、19.20`；主要机会在调度、并行粒度、Matmul 流水和 K 轴并行。
- 第三优先级：case 3、4、13、14；只接受不会拖累前两组的通用收益。

本文件中的“20 次尝试”是 **最多 20 个可提交的架构实验槽位**，不是要求无条件提交 20 次。每个槽位都必须先满足准入条件；前置证据不成立就取消该槽位，把机会留给有依据的分支。

## 2. 不可破坏的硬约束

所有实验同时满足：

1. 每次迭代只 launch 1 个 global kernel；所有阶段都必须在同一个 MIX kernel 内完成。
2. batch 一一配对，禁止 broadcast。
3. 归约顺序始终是先沿 N 求 Max，再沿 M 求 Sum；Split-K 只能发生在点积内部，并必须先完成 K 合并。
4. `transposeX1/transposeX2` 只描述 storage shape，不执行额外逻辑转置。
5. FP16/BF16 输入均以 FP32 累加、归约并输出；结果必须可复现。
6. 不按隐藏 case 编号或精确 shape 硬编码分支。路由条件只能来自一般特征，例如 M/N/K、dtype、layout、对齐、tile 数和可用核数。
7. 新路径失败时必须回退到已验证路径，不能返回伪结果或静默跳过计算。

## 3. 提交纪律：先证明值得投，再占用次数

### 3.1 不消耗提交次数的前置工作

在尝试 01 前完成：

- 从历史探针和提交详情恢复每个 case 的 `M/N/K` 桶、batch 桶、dtype、四种 storage layout、对齐情况以及当前实际路由。
- 建立 `case -> 特征 -> 当前路径 -> 主要开销假设` 表；未知项明确标为未知，不能靠猜测补齐。
- 用官方模板和当前安装版本文档核对将使用的 Matmul/Vector API；未确认签名、事件语义、workspace 契约的 API 不进入代码。
- 扩充本地参考测试：四种 layout、两种 dtype、全负数、重复最大值、M/N/K 尾数、K 非 16 对齐、batch 配对和多次重复一致性。
- 固化基线源码、15 个耗时、源文件 hash 和远端 submission ID。

若历史信息仍不足，只允许设计 **一次能同时回答多个架构决策的正交探针**；它必须替换下列某个实验槽位，不能额外增加提交数。禁止再为 M/N 已知区间重复探针。

### 3.2 每次实验的固定流程

```text
最近一次 15/15 Pass
  -> 写下瓶颈证据与预计影响 case
  -> 只改变一个架构机制
  -> 静态检查 launch / layout / workspace / barrier
  -> 本地 reference test
  -> 核对上传源码 SHA-256
  -> 提交一次
  -> 记录 15 个耗时和正确率
  -> Keep 或完整 Revert
```

### 3.3 准入、保留与止损阈值

- **准入提交**：先用官方对数公式逐 case 估计影响；架构实验应有可解释的目标耗时改善，原则上预期平均分 `+0.08` 以上，或能解锁后续大收益。窄路由的低风险改动可在有明确机制、目标收益和自然复验计划时作为例外，不能用未命中 case 的偶然变快补足预期。
- **正确性门槛**：必须 15/15 Pass。14/15 也不在错误版本上继续叠加优化。
- **性能保留**：以目标 case 的命中证据和官方公式分值变化为主；优先要求目标 case 稳定快 `10%`、整体不下降。窄路由的 5% 级收益只有在正确性可靠、未命中路径不变、下一次不同机制提交能自然复验时才暂留；不再以错误的线性估分设置固定 `+0.25` 门槛。
- **回退限制**：任何非目标 case 回退超过 `8%`，或总体变慢，默认回退；除非能明确证明是评测噪声并且预计下游收益更大。
- **全 0 处理**：先判定编译、设备资源和启动问题；没有外部服务故障证据时不重复同一 hash。`379213/379331` 已证明“全 0”可以是确定性代码问题。
- **分支止损**：父实验的机制未获得正收益，依赖它的子实验全部取消，不用更多提交“救活”错误方向。

## 4. 依赖图与额度分配

```text
离线特征表
  ├─ 调度/大 tile：01 -> 02 -> 03 -> 04
  ├─ N 并行流水：05 -> {06, 07 -> 08} -> 09
  ├─ Epilogue/小 shape：10 -> 11；12 -> {13,14 -> 15} -> 16
  ├─ K 轴：17 -> 18；必要时 -> 19
  └─ 只合并已获胜分支：20
```

建议额度：调度与 supertile 4 次、N 并行 5 次、Epilogue/小 shape 7 次、K 轴 3 次、最终集成 1 次。某一阶段提前证伪后，剩余额度转给已经出现正收益的阶段，不为了凑数继续投。

## 5. 二十个候选实验

### 阶段 A：重新设计调度和 Matmul 工作粒度

#### 尝试 01：基于代价模型选择 Batch / M-parallel / N-parallel

- **目标**：case 7、10、11、12、13，以及历史上因固定策略走错路径的 shape。
- **机制**：Host 端不再只用 `nGroups > 1` 和 `blockCount >= batch * 2` 两个阈值。枚举三种合法执行方式，根据 `mTiles × nTiles × batch`、K、尾块、活跃核数、partial 写回量和 barrier 次数估算成本，选择预计最小者。
- **为什么不是盲试**：case 11 强制 `nGroups=1` 已实测从约 225 us 退化到 331 us，说明并行度和同步成本必须联合权衡；现有二元规则无法表达这一点。
- **准入条件**：特征表能列出每个 case 当前走哪条路径，并能解释至少两个低分 case 的资源浪费。
- **验收**：15/15；至少两个明确命中的目标 case 快 10%，按官方公式评估整体收益；未命中 case 的波动不计入机制收益。
- **止损**：如果只有单个 case 在噪声范围内变化，则回退，不继续围绕阈值扫参。

#### 尝试 02：自适应 N-group 粒度，保证每组有足够工作

- **目标**：N 很大但当前每组只有一个 N tile、partial/SyncAll 成本过高的 case。
- **机制**：从合法 `nGroups` 候选中选择：同时约束核心占用率和每组 tile 数，优先让每个 worker 连续处理至少两个 N tile，再以实算工作量而不是 `coreCount / mJobs` 最大化分组数。
- **依据**：连续 N group 曾让 case 12 从约 285 us 降到约 223 us；反之无条件降到 1 组又让 case 11 大幅变慢。应寻找中间的工作粒度，而不是二选一。
- **依赖**：尝试 01 的代价模型至少能正确分类目标 case。
- **验收**：N-parallel 目标组整体快 10% 以上，case 11 不得回退超过 5%。
- **止损**：只允许一个公式版本；不做 2/4/8 等无依据枚举提交。

#### 尝试 03：连续 M-supercall，跨多个 M tile 复用 B

- **目标**：M tile 多、N tile 少或中等的 case，尤其 case 12、13。
- **机制**：类似已经成功的连续 N-group，把相邻 M tiles 合并成一次较大的 Matmul 输入区间，按 Matmul 的确定输出顺序逐 tile 消费 C，减少重复 `SetTensor`/`Iterate` 和 B 的重复搬入。
- **准入条件**：用官方 Matmul 文档/样例确认多 M tile 的输出迭代顺序和 `GetTensorC` stride；先限制在无 M/N 双尾、K 对齐的窄条件。
- **验收**：路由到该路径的 case 至少快 15%，其它 case 完全不变。
- **止损**：若输出顺序或 tiling stride 无法在本地静态证明，取消，不上机碰运气。

#### 尝试 04：二维 M×N supertile，一次 Matmul 调用覆盖多个 C tile

- **目标**：M、N 均较大且重复设置开销明显的 case 10、11、13、14。
- **机制**：在尝试 03 已证明连续 M 消费正确的前提下，让一次 Matmul 同时覆盖多个 M 与 N tile；C tile 到达后立即完成 Max(N) 累积，避免完整 C 落 GM。
- **依赖**：尝试 03 必须正确且有实测收益；否则取消。
- **验收**：目标 case 合计按官方公式有明确正收益，无额外 global kernel、无完整 B×M×N workspace；同时核对超块减少的活跃核数。
- **风险**：C tile 输出次序、M/N 双尾和源码模板体积。首版只覆盖规则对齐区域，尾块走基线。

### 阶段 B：重构 N-parallel，而不是继续调一个参数

#### 尝试 05：N-group 使用唯一一份异步 Matmul 消费循环

- **目标**：case 7、10、11 等 N-parallel 主耗时 case。
- **机制**：先把同步/异步消费抽成共享的小型结构，随后仅在满足连续 N tile、K 对齐、workspace 足够时，把 `Iterate<true>` 改为 `Iterate<false>` 并与 `GetTensorC<false>` 成对使用。
- **依据**：主 `ProcessMTile` 异步化已实测显著改善 case 9/10/11；以前失败的原因是复制多套大函数造成资源膨胀，不是异步机制本身无效。
- **准入条件**：设备代码不能复制第二套完整尾块/归约实现；workspace 按 block 隔离并由实际 tiling 计算。
- **验收**：15/15；至少一个 N-parallel case 快 12%，源码/UB/GM workspace 增量在预算内。
- **止损**：全 0 或资源异常立即回退；不通过继续删几行然后重复提交。

#### 尝试 06：C 输出双缓冲，重叠下一 tile 的 Cube 与当前 tile 的 Vector 归约

- **目标**：尝试 05 中确认存在 Cube/Vector 串行等待的 case。
- **机制**：只给 Matmul C 输出设置两个明确所有权的 buffer，形成 `Cube(tile i+1)` 与 `ReduceMax(tile i)` 的 ping-pong；tail buffer、rowMax 等不盲目双缓冲。
- **依赖**：尝试 05 正收益，并完成 UB liveness 预算；否则取消。
- **验收**：目标 case 在 05 基础上再快 8%，不得因 UB 增长导致其它 case occupancy 下降。
- **止损**：如果 profiler/时序推断显示 Vector 不是关键路径，不提交。

#### 尝试 07：一个 block 处理相邻多个 N segment，并在 UB 内先合并

- **目标**：partial 写回与 barrier 比例过高、但完全串行 N 又损失 Cube 并行度的 case。
- **机制**：把若干相邻 N segment 分配给同一 worker，复用同一 A tile，先在 UB 合并 rowMax，只写一次 partial；减少 A 的 GM/L1 重载、partial 数量和 owner 合并次数。
- **与尝试 02 的区别**：02 只改变调度粒度；07 改变 worker 内部数据复用和 partial 产生方式。
- **验收**：partial 字节数至少下降 2 倍，目标 case 快 10% 以上，同时保持足够活跃 Cube 核。
- **止损**：若特征表显示目标 case 已经每组包含多个连续 tile且 partial 不占主导，取消。

#### 尝试 08：N partial 的条带化批量搬入与向量 Max

- **目标**：nGroups 多、owner 合并循环明显的 case。
- **机制**：为 `(mTile, group, row)` 重新设计可连续读取的 partial 布局；owner 每次批量搬入一个受 UB 限制的 group stripe，再进行向量 Max，减少多个短 `DataCopyPad` 和队列往返。
- **依据**：此前失败/无收益的是 **M partial 的 ReduceSum**；这里是 N-group 的多向量 Max，访问模式与数据量不同，只有在测得 merge 占比高时才成立。
- **依赖**：尝试 07 后仍有显著 owner merge 成本；UB 能容纳至少两个 group stripe。
- **验收**：目标 case 再快 8%，生产者写入不能变成大量非对齐标量写。
- **止损**：若布局优化只把读端成本转移到写端，立即回退。

#### 尝试 09：N-parallel 胜出机制集成版

- **目标**：验证 05–08 的机制能否共同存在，而不是分别快、合并后资源爆炸。
- **机制**：只合并已单独获胜的异步、worker 局部合并和批量 merge；重新做 UB/L1/workspace 生命周期规划并删除被替代旧路径。
- **准入条件**：05–08 至少两个分支各自带来明确正收益；若只有一个赢家，本槽取消，直接保留该赢家。
- **验收**：合并后的整体成绩不低于单项最佳，15/15，源码不保留重复实现。
- **止损**：若组合收益互相抵消，保留贡献最大的单项，不再为组合消耗提交。

### 阶段 C：Epilogue 与小 shape 专用计算路径

#### 尝试 10：主路径 C 输出 ping-pong，而非增加第二套 Matmul 函数

- **目标**：当前走 `ProcessMTile` 且 `nTiles > 1` 的 case 5、9、10 等。
- **机制**：在已经验证的异步主路径内，把单槽 `mmOutputQueue_` 改成严格配对的双槽消费；当前 tile 做 Max 时让下一 tile 的 C 输出推进。
- **准入条件**：确认评测设备实际 UB 大小；依据实际 `baseM/baseN` 计算包含队列、尾块、同步和归约缓冲的完整 UB 预算。对 128×128 FP32 tile，两个 C buffer 共需 128 KiB；若仍分配 64 KiB 尾块 buffer，已达到 A2 文档的 192 KiB 上限，尚未计算其它 buffer。仅对可证明永不进入 padded 尾块路径的形状，才可考虑不初始化尾块 buffer 的窄门控版本；还须证实主路径剩余关键路径确为 GetTensorC/Vector 串行等待，并核对双槽所有权及 Matmul 生成 stride。当前均未满足，不提交。
- **验收**：至少两个主路径 case 快 8%，其它路径不初始化第二槽或不受影响。
- **止损**：如果 UB 占用导致任何大 case 明显变慢，回退，不继续给更多队列加双缓冲。
- **实测结论**：提交 `390882` 在 Host 查询实际 UB 后仅对 `nGroups=1、nTiles>1、M/N 整 base tile 且 8 对齐、UB 预算足够` 的主路径启用两槽；15/15 Pass，但按官方公式约 `20.328`，低于父版 `20.471`，无两个明确命中 case 获得 8% 收益。已完整回退，除非新 profiler 证明 Vector/MTE 到 C 的等待仍是关键路径，否则不再继续调此方案。

#### 尝试 11：尾块原生 masked epilogue，移除整块 padded 复制

- **目标**：M/N 非对齐、当前频繁 `Duplicate + Adds + 标量补 -FLT_MAX` 的 case。
- **机制**：按官方 Vector mask/归约契约，只让合法 lane 参与 ReduceMax；或按 8/64 lane 分段归约，尾 lane 显式置最小值，但不复制整个 `baseM × baseN` C tile。
- **准入条件**：能对全负输入、N<8、N 尾 1–7、M/N 双尾给出逐 lane 正确性证明；使用的 mask API 在当前 CANN 版本已确认。
- **验收**：所有尾数测试通过；命中 case 快 12%，对齐 case 不变。
- **止损**：只要需要未验证的跨 stride mask 语义就取消，保留当前 padded 安全路径。
- **当前审计**：官方 CANN9.0 高阶 `ReduceMax<AR>` 在 A2/A3 的内轴仅支持 `srcInnerPad=true`，宽度非 32 字节对齐时必须有物理 padding；基本 masked `ReduceMax` 虽可限制有效 lane，但它给的是单次总体归约，不能直接代替按每一行输出的高阶 AR。须先证明逐行/多 repeat 的输出布局、临时空间、全负行和最终 Max/Sum，再考虑实现；本轮不提交。

#### 尝试 12：连续 layout 的 FP16 AIV 直算原型

- **目标**：M/N 小或 K 很小、Cube 固定开销占主导的低分 case 1、2、5，以及特征表确认适合的 case 12。
- **机制**：仅覆盖 `x1` 逻辑行和 `x2` 逻辑 document 向量在 storage 中都沿 K 连续的 layout。AIV 分 K chunk 搬入、转换为 FP32、乘加完成一个或一组 dot，随后严格 Max(N) 再 Sum(M)。仍在同一个 kernel 内。
- **为什么先做这一 layout**：它不需要 gather/转置，可先判断“绕开 Cube 固定开销”这一核心假设是否成立。
- **准入条件**：特征表确认至少一个高价值 case 会命中；建立计算量阈值，预计 AIV 指令和搬运少于 Cube 设置成本。
- **验收**：命中 case 快 20% 以上且数值一致；未命中 case 保持基线路径。
- **止损**：若命中 case 不快，后续 13–16 全部取消，因为增加 layout 支持只会更贵。

#### 尝试 13：将直算路径扩展到 BF16，但继续 FP32 累加

- **目标**：尝试 12 命中的 BF16 小 shape。
- **机制**：复用同一套 K-chunk/Max/Sum 结构，仅增加官方支持的 BF16→FP32 搬入与转换，不复制整套算法。
- **依赖**：12 对 FP16 已有明确收益，且历史特征表证明存在高价值 BF16 命中。
- **验收**：BF16 15/15 精度要求通过，命中 case 相对 Cube 快 15% 以上。
- **止损**：若转换成本吃掉收益，BF16 保持 Cube，不为统一代码强推直算。

#### 尝试 14：x2 非连续 layout 的 UB 小块转置/重排

- **目标**：`x2` 的 K 方向不连续而无法使用尝试 12 的 case。
- **机制**：一次搬入 K×N 小块，在 UB 内转为多个连续 document 向量；同一 x2 小块复用于多个 query 行，重排成本由 M 方向复用摊薄。
- **准入条件**：官方 DataCopy/Transpose 路径已核对，K×N 小块对齐且 UB 预算明确；预计复用次数足以覆盖重排成本。
- **验收**：目标 layout 至少两个 case 或一个高权重 case快 15%；连续 layout 不回退。
- **止损**：只命中一次且性能低于 Cube则取消，不继续扩大 tile。

#### 尝试 15：x1 非连续 layout 的 UB 分块重排

- **目标**：`x1` 逻辑行沿 K 非连续的剩余 layout。
- **机制**：搬入 K×M 小块并在 UB 重排为连续 query 向量，再复用到多个 N dot；与尝试 14 共享微内核，不新增硬编码 fallback。
- **依赖**：12 或 14 已证明 AIV 直算机制有足够收益；特征表确认该 layout 值得覆盖。
- **验收**：目标 layout 快 15% 以上，四种 storage shape 全正确。
- **止损**：M 很小导致重排无法摊薄时，保留 Cube；不为了“全 layout 都走新路径”牺牲性能。

#### 尝试 16：二维微 tile + K 双缓冲的小 shape 完整路径

- **目标**：直算分支已经获胜但仍受重复加载 x1/x2 或 MTE2 等待限制的 case。
- **机制**：一个 block 同时处理 `mMicro × nMicro` 个 dot：x1/x2 K-chunk 各加载一次，在多个 dot 间复用；两组 K buffer 交替搬入和计算，最后按行 Max、按 M Sum。
- **依赖**：12–15 至少一个 layout 有显著收益，且 liveness 分析证明双缓冲不会挤掉 FP32 accumulators。
- **验收**：在最佳直算版本上再快 10%，且阈值由计算量模型决定而不是精确 shape 列表。
- **止损**：寄存器/UB 压力导致 occupancy 或编译资源异常时，回到单缓冲获胜版。

### 阶段 D：大 K 与低并行度场景

#### 尝试 17：只对已证明的大 K 场景启用受支持的 MDL 配置

- **目标**：特征表和已有耗时共同指向 K 搬运/L1 等待主导的 case，预计包括 case 5、10、11 中的部分 shape。
- **机制**：依据当前 CANN 版本官方 Matmul tiling 文档配置 MDL/多 stage；完整设置与之匹配的 `baseK/stepK/depthA1/depthB1`，而不是只翻一个 preload 开关。
- **准入条件**：打印/审计实际 tiling 结果，确认 L1/L0 预算、K 对齐、layout 支持和 workspace；条件不满足继续走基线。
- **验收**：大 K 目标组快 12%，非大 K 不进入新模板。
- **止损**：过去“盲开 MDL”全 0，本次若无法证明完整 tiling 契约，直接取消。

#### 尝试 18：显式 K-stage 双缓冲与 A/B 搬运错峰

- **目标**：17 证明 K/MTE2 是瓶颈，但通用 MDL 仍未充分重叠搬运与 Cube 的场景。
- **机制**：在受控 tiling family 内安排 A/B 的 K 分块、L1 stage 和深度，使 stage i 计算时预取 stage i+1；优先复用 A 或 B 中复用次数更高的一侧。
- **依赖**：17 至少显示大 K case 的方向性收益；官方样例中能找到同芯片/同 API 代际可迁移的配置。
- **验收**：在 17 上再快 8%，实际生成 tiling 与设备侧 stride 完全一致。
- **止损**：不做 baseK/stepK 数值网格搜索。成本模型不能区分候选时不提交。

#### 尝试 19：单 kernel 内的确定性 Split-K 两阶段路径

- **目标**：M×N 很小、K 很大，现有 M/N 并行无法用满核心的 case；这是高风险后备方案，不是默认路线。
- **机制**：同一个 global kernel 内由多个 K group 生成 FP32 dot partial，写入互不重叠 workspace；一次全局同步后，owner 按固定 K-group 顺序合并完整 dot，再执行 Max(N) 和 Sum(M)。不得在 K 合并前做 Max。
- **准入条件**：特征表明确存在低并行度大 K 高价值 case；估算 partial workspace 和额外 GM 流量后仍有至少 20% 理论收益；SyncAll 已有稳定实现可复用。
- **验收**：15/15、多次结果一致，目标 case 快 20% 以上；一次 kernel launch。
- **止损**：若需要存完整大尺寸 C partial、workspace 过大或 barrier 成为主导，取消。禁止使用非确定性原子归约绕过同步。

### 阶段 E：最终整合

#### 尝试 20：仅整合赢家，删除被替代路径并重新做全局资源规划

- **目标**：得到最终候选，而不是把 19 个实验代码全部堆进一个 kernel。
- **机制**：只保留实测提高总分的分支；共享 tail、reduction 和 Matmul 消费代码；删除关闭的探针、失败模板和重复 buffer。重新计算每条路径的 UB/L1/GM workspace 与 blockCount，确保模板体积和设备资源不超过已验证边界。
- **准入条件**：至少有两个互不冲突的获胜机制；若只有一个，最终版就是该单项，不强行集成。
- **验收**：15/15；恰好一次 launch；最终粗估分不低于历史最好版本；对潜在噪声 case 做一次受控复验后再定版。
- **最终目标**：先稳定越过 30，再继续以相同证据闭环追求 35/40；不因达到 30 停止，也不为了冲分牺牲可复现性。

## 6. 明确不再消耗提交次数的方向

以下方向已有反证，除非出现新的 profiling/官方文档证据，否则不进入上述 20 次：

- 多 global kernel 或 host 两阶段计算：违反每迭代恰好一次 launch。
- 再次提交 `17<=M<=32 -> baseM=16`：同 hash 两次全 0，已确定性证伪。
- 对大 BF16/跨 layout 统一 `nGroups=1`：case 11 已实测大幅退化。
- 全局切换 `CFG_NORM`、不完整的 MDL preload、强制 tiling 与实际 tiler 输出不一致。
- 把异步 Matmul 整套复制到多个大函数；曾造成代码/资源膨胀和全 0。
- M-parallel scalar partial merge 的再次向量化：正确版本无可测收益。
- 只缩 UB、只改 Host malloc/memset、只减少循环判断等微优化：已证明不足以显著提高得分。
- 原子 Max/Sum、无固定次序的跨核归约：可能破坏数值一致性。
- 精确 case 编号、隐藏 shape 或答案硬编码。

## 7. 每次提交后的记录模板

```text
尝试编号：
父基线 submission / SHA-256：
唯一架构变化：
目标 case 与瓶颈证据：
路由条件（一般 shape 特征）：
本地测试：
静态资源检查：
远端 submission ID：
15 个 precision：
15 个 time：
粗估平均分变化：
结论：Keep / Revert / Cancel descendants
下一步：
```

## 8. 执行顺序的检查点

- **Checkpoint A（尝试 01–04 后）**：调度/supertile 必须在明确命中的 case 上取得可复现的两位数耗时改善，且按官方公式总分为正；否则停止这一阶段。
- **Checkpoint B（尝试 05–11 后）**：N-parallel 与 epilogue 至少有一个获得两位数百分比收益，否则把余额转给小 shape 路径。
- **Checkpoint C（尝试 12–16 后）**：直算路径若不能让任何低分 case 快 20%，整条路线终止，不扩 layout。
- **Checkpoint D（尝试 17–19 后）**：只有确证大 K/低并行度瓶颈才保留 K 专用路径。
- **Checkpoint E（尝试 20）**：最终版本以 15/15、一次 launch、总分历史最好为唯一发布标准。

这套计划的核心不是“把 20 次用完”，而是让每一次实际提交都回答一个明确问题，并让失败立即裁掉一整条无效分支。

## 9. 2026-09-21 未执行槽位复核

今日用一单受控 GEMV 扩围检验了小 M、大 N 的通用路由：`394284` 15/15 Pass，但主要低分目标 case 5 未快，已回退；详见 `memory.md` 8.16。计划 11 进一步核对官方基础 ReduceMax：逐行归约虽可对齐输入，但输出每行要求 8 字节起址，不能直接写紧凑 `tileMax[row]`；还要补 8-stride 聚集，缺少性能准入证据，详见 8.17。

目前没有另一个同时满足“明确目标、已确认 API/资源契约、预计至少有可解释的收益、一次 launch、可本地验证”的待提交槽位。01/02 缺隐藏 case 的当前 tile/route 特征，03/04 缺输出 stride 与活跃核证明，06/08 缺关键路径数据，11 缺输出聚集和收益证明，17/18 缺实际 tiling 与 MTE2/L1 数据，19 缺低并行度大 K case 与 workspace/流量预算；09/20 又要求至少两个独立赢家。13–16 已随 12 的反证取消。下一次提交需要首先取得这些缺失证据中的至少一项，不以“计划还有编号”作为提交理由。

## 10. 放宽准入后的实验与协作状态（2026-09-22）

用户允许对原先缺证据的方向做大胆单机制实测，前提仍是一次 launch、可回退且结果逐项记录。计划 02 的双 tile N-group 候选 `394474` 与计划 11 的原缓冲 N-tail 填充候选 `394588` 都返回 15 项全 0 Wrong Answer；平台未提供编译/设备日志，不能推断性能，两项均已回退。详细源码 hash 与过程见 `memory.md` 8.18–8.19。当前 `kernel.asc` 仍为 384543 的 15/15 Pass 可靠基线，不把失败候选交给队友当作可提交版本。

协作时先从本仓库 `main` 同步，单独分支实验。候选只有在真实 15/15 Pass 且目标 case 耗时改善可解释时才替换 `kernel.asc` 的可靠基线；失败记录写入 `memory.md`，不覆盖队友的 `cann_api_reference.md`、`new_design.md`。
